[project]
name = "intro-deep-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.10.1",
    "scikit-learn>=1.6.1",
]

[dependency-groups]
pytorch = [
    "torch>=2.6.0",
]
keras = [
    "keras>=3.9.2",
]
